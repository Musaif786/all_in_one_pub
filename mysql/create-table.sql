CREATE TABLE students(
id INT PRIMARY KEY,
name VARCHAR(100),
age INT NOT NULL
);

use musaif;
create table m(
name varchar(100),
rollNo int primary key)

select * from m;

insert into m(name,rollNo) values("Musaif",10);